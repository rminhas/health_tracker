name: health_tracker
description: A new Flutter project for tracking health.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  sqflite: ^2.3.0
  path_provider: ^2.1.2
  provider: ^6.1.1
  shared_preferences: ^2.2.2
  http: ^1.6.0
  path: ^1.9.1
  health: ^13.3.1
  mobile_scanner: ^7.2.0
  permission_handler: ^12.0.1
  fl_chart: ^1.2.0
  table_calendar: ^3.2.0
  share_plus: ^10.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  sqflite_common_ffi: ^2.3.4

flutter:
  uses-material-design: true
