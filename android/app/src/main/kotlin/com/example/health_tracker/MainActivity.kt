package com.example.health_tracker

import io.flutter.embedding.android.FlutterFragmentActivity

// FlutterFragmentActivity (not FlutterActivity) so the `health` plugin can cast
// to androidx.activity.ComponentActivity to register a permission launcher.
class MainActivity : FlutterFragmentActivity()
